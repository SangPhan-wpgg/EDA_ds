# Bảng báo cáo chi tiết các biểu đồ và bảng số liệu trong `yp2021_report.ipynb`

Báo cáo phân tích dữ liệu Youth Panel 2021 theo câu hỏi trung tâm: dự đoán tình trạng có việc sau tốt nghiệp (`employed_w04`) của thanh niên Hàn Quốc trong mẫu pilot wave 4. Dữ liệu chính gồm 5.687 respondent và 47 cột, tỷ lệ có việc là 75,6%.

## 1. Biểu đồ

| STT | Cell | Biểu đồ | Dữ liệu/biến dùng để vẽ | Vì sao vẽ được như vậy | Ý nghĩa và điều cung cấp |
|---:|---:|---|---|---|---|
| 1 | 5 | Funnel từ khung chọn mẫu A đến mẫu phân tích S | `analysis_sample_funnel.parquet`, ba bước chính: `w04_frame` = 12.213, `w04_participated` = 9.665, `post_graduation_scope` = 5.687 | Đếm số respondent còn lại sau từng điều kiện lọc: toàn bộ wave 4, có tham gia wave 4, rồi thuộc phạm vi sau tốt nghiệp (`w04edu >= 3`). Dùng bar chart vì đây là chuỗi số lượng rời rạc theo bước lọc. | Cho thấy mẫu phân tích không phải toàn bộ dữ liệu thô. Từ 12.213 người trong khung wave 4, còn 9.665 người phản hồi và 5.687 người đúng phạm vi phân tích. Biểu đồ giúp giải thích thiên lệch chọn mẫu và giới hạn suy rộng. |
| 2 | 7 | Attrition panel wave 1 -> wave 4 | `panel_participation_summary.parquet`: wave 1 = 12.213, wave 2 = 10.721, wave 3 = 10.108, wave 4 = 9.665 | Dữ liệu panel có cùng khung người qua nhiều wave. Vẽ line chart theo `wave` và `participated` để thấy xu hướng rơi rụng theo thời gian. | Retention wave 1 -> wave 4 là 79,1% (9.665/12.213). Biểu đồ cho biết rủi ro attrition: người còn lại ở wave 4 có thể khác người rời panel, nên kết quả chịu rủi ro sai lệch không phản hồi. |
| 3 | 13 | Mô phỏng urn/Binomial so với xấp xỉ Normal | `samp.urn_draws`, `samp.rate_point`, `samp.analytic_se`; n = 5.687, p-hat khoảng 0,756 | Xem mỗi respondent như một phép thử Bernoulli: có việc = 1, không việc = 0. Mô phỏng nhiều mẫu Binomial rồi chồng đường Normal với trung bình p-hat và sai số chuẩn giải tích. | Chứng minh tỷ lệ có việc 75,6% có độ bất định lấy mẫu nhỏ, CI 95% khoảng 74,5%-76,8%. Vì n lớn, mô phỏng Binomial và xấp xỉ Normal gần trùng nhau. |
| 4 | 15 | Độ bất định của chất lượng mô hình | Bootstrap ROC-AUC trên test set và AUC qua 80 lần split 80/20 | Histogram trái lấy lại test set bằng bootstrap để tạo phân phối ROC-AUC; histogram phải thay đổi ngẫu nhiên train/test split để đo độ nhạy với cách chia dữ liệu. | ROC-AUC trung bình khoảng 0,68 và CI bootstrap loại trừ 0,5, nghĩa là mô hình có tín hiệu phân loại thật nhưng yếu. Split variation có std khoảng 0,016, cho thấy kết quả tương đối ổn định, không phụ thuộc duy nhất một lần chia dữ liệu. |
| 5 | 29 | Ba biểu đồ EDA cơ bản: target, số lần đổi việc, thời lượng việc trung bình | `employed_w04`, `job_changes`, `avg_job_duration_months` | Bar chart dùng cho target nhị phân; histogram dùng cho biến đếm/biến liên tục sau khi bỏ missing. | Target lệch mạnh về lớp có việc, nên accuracy dễ bị thổi phồng. `job_changes` và `avg_job_duration_months` cho hình dạng phân phối kinh nghiệm việc làm, trong đó duration có nhiều missing và lệch phải. |
| 6 | 31 | Small multiples phân phối biến phân loại | 11 biến phân loại: giới tính, vùng, học vấn, loại trường, ngành, GPA nhóm, chứng chỉ, chuẩn bị việc, học vấn cha/mẹ, tài sản cha mẹ | Với mỗi biến, lấy `value_counts`, giữ tối đa 8 mức phổ biến, vẽ bar ngang và ghi tỷ lệ thiếu. | Cho biết cấu trúc mẫu theo từng nhóm. Một số biến chi tiết như ngành, loại trường, GPA nhóm có độ phủ hẹp, nên chỉ dùng cho phân tích nhạy cảm thay vì mô hình core. |
| 7 | 32 | Small multiples phân phối biến số | `age_w04`, `graduation_gpa_yp100`, `job_changes`, `avg_job_duration_months`, `total_jobs` | Mỗi biến số được bỏ missing rồi vẽ histogram 25 bins để quan sát dạng phân phối. | Tuổi có độ phủ đầy đủ và khá tập trung; GPA chỉ có 946 quan sát; job history có phân phối lệch và không đầy đủ. Biểu đồ giúp quyết định biến nào phù hợp đưa vào model và biến nào cần biến đổi/loại trừ. |
| 8 | 34 | Coverage theo biến processed | `yp2021_processed_coverage.parquet`: `valid_rate`, `valid_count`, `output_column` | Sắp xếp biến theo tỷ lệ valid tăng dần; tô đỏ các biến dưới 50%; kẻ ngưỡng 0,5. | Cho thấy biến `job_prep_gpa_spec` chỉ valid 13,6%, GPA/loại trường/ngành khoảng 16,6%, `avg_job_duration_months` khoảng 30,2%. Đây là cơ sở để không lạm dụng các biến độ phủ thấp và để tách mẫu chính/nhạy cảm. |
| 9 | 40 | So sánh độ lệch trước/sau biến đổi | Bảng transformation candidates: `raw_skew`, `skew_after`, `recommended_transform` | Tính skewness gốc, thử `log1p` hoặc `sqrt`, rồi vẽ bar ngang của `|skew|` trước và sau biến đổi, kèm ngưỡng 0,5. | `avg_job_duration_months`, `total_jobs`, `job_changes` lệch phải và có thể cần biến đổi. GPA và tuổi không cần biến đổi mạnh. Biểu đồ giải thích vì sao một số biến số nên log/sqrt trước khi đưa vào mô hình tuyến tính. |
| 10 | 42 | Tỷ lệ có việc theo nhóm phân loại | 11 biến phân loại và target `employed_w04` | Với từng biến, groupby theo nhóm, tính mean của target, loại nhóm có `count < 20`, vẽ bar ngang; kẻ đường tỷ lệ chung 75,6%. | Cho thấy nhóm nào có tỷ lệ có việc cao/thấp hơn trung bình. Đây là EDA liên hệ thô, giúp phát hiện nhóm ngành, vùng, chứng chỉ, học vấn gia đình có thể liên quan tới outcome. Không được đọc là quan hệ nhân quả. |
| 11 | 46 | Heatmap tương quan Pearson giữa biến số | `numeric_correlation(df)`, gồm target dạng 0/1 và các biến số | Tính hệ số Pearson giữa các biến số, rồi vẽ ma trận màu `RdBu_r` từ -1 đến 1 và ghi số trong từng ô. | Cho biết biến số nào đi cùng target và biến số nào trùng thông tin với nhau. Tuổi là biến số có liên hệ mạnh nhất với target trong bảng association, point-biserial khoảng 0,241. |
| 12 | 47 | Xếp hạng liên hệ từng đặc trưng với outcome | `feature_target_association(df)`; numeric dùng `|point-biserial r|`, categorical dùng Cramer's V | Chuẩn hóa mọi đặc trưng về một thang “strength” rồi vẽ bar ngang. Màu xanh dương là numeric, xanh lá là categorical. | Xếp hạng mạnh nhất: tuổi (0,241), nhóm ngành hẹp (0,152), thời gian TB/việc (0,114), vùng cư trú (0,079), chứng chỉ (0,072), tổng số việc/đổi việc (0,072). Đây là liên hệ một biến, chưa kiểm soát biến nhiễu. |
| 13 | 51 | Heatmap liên hệ đa biến giữa predictor phân loại | `de.multivariate_association(df)`, Cramer's V giữa các predictor phân loại | Tính Cramer's V cho từng cặp biến phân loại, vẽ heatmap `YlOrRd` từ 0 đến 1. | Giúp phát hiện các predictor trùng thông tin, ví dụ học vấn, loại trường, ngành hoặc nền tảng gia đình có thể liên quan nhau. Cặp có Cramer's V cao báo hiệu đa cộng tuyến hoặc thông tin lặp. |
| 14 | 62 | Ma trận nhầm lẫn của 4 mô hình | Kết quả `run_modeling`: baseline, Logistic L2, Logistic L1, Random Forest | Mỗi mô hình tạo ma trận 2x2 trên test set với nhãn thật 0/1 và dự đoán 0/1. Vẽ heatmap nhỏ cho từng mô hình. | Baseline dự đoán toàn bộ là “có việc”: [[0,277],[0,861]], nên accuracy 75,7% nhưng không phát hiện lớp không việc. Logistic L2: [[19,258],[15,846]], Logistic L1: [[19,258],[14,847]], Random Forest: [[171,106],[286,575]]. Random Forest bắt lớp không việc tốt hơn nhưng bỏ sót nhiều người có việc. |
| 15 | 64 | ROC curve và Precision-Recall curve | `roc_curve`, `precision_recall_curve`, AUC/AP của Logistic L2, Logistic L1, Random Forest | ROC vẽ TPR theo FPR ở mọi ngưỡng; PR vẽ precision theo recall. Đường nền ROC là 0,5; đường nền PR là prevalence. | AUC của ba mô hình thật khoảng 0,679-0,681, tốt hơn ngẫu nhiên nhưng không mạnh. Average precision khoảng 0,853-0,855, cần đọc cùng prevalence cao 0,756. |
| 16 | 66 | Calibration curve và threshold tuning | Xác suất dự đoán `y_proba`, Brier score, bảng F1 theo threshold | Calibration chia xác suất dự đoán thành 8 bin quantile, so trung bình dự đoán với tỷ lệ quan sát. Threshold tuning vẽ F1 theo ngưỡng quyết định. | Logistic có Brier khoảng 0,170, Random Forest khoảng 0,220. Biểu đồ trả lời: xác suất dự đoán có đáng tin không, và ngưỡng 0,5 có phải lựa chọn tốt nhất nếu mục tiêu là F1 hay không. |
| 17 | 69 | Hiệu năng mô hình Logistic L2 theo nhóm | `results.subgroup_frame` cho giới tính, vùng, học vấn; metric ROC-AUC từng nhóm | Lọc model L2, group theo từng subgroup có n >= 30, vẽ AUC từng nhóm và kẻ AUC tổng thể 0,679. | Kiểm tra công bằng/độ đồng đều. Gap AUC: học vấn 0,033, giới tính 0,020, vùng 0,075. Mô hình tương đối đồng đều, nhưng vùng có dao động lớn hơn. |
| 18 | 71 | Top 15 hệ số log-odds Logistic L2 | `results.coefficients`, bỏ intercept, xếp theo `abs(log_odds)` | Hồi quy logistic cho hệ số trên log-odds. Bar xanh là hệ số dương, cam là âm. | Biến lớn nhất là tuổi (`log_odds` 0,653; odds ratio 1,92). Một số mức học vấn/tài sản/vùng/chứng chỉ có hệ số âm hoặc dương. Cần đọc là liên hệ dự đoán sau mã hóa và regularization, không phải tác động nhân quả. |

## 2. Bảng số liệu và output dạng bảng

| STT | Cell | Bảng/output | Nội dung chính | Vì sao tính được | Ý nghĩa và cách đọc |
|---:|---:|---|---|---|---|
| 1 | 3 | Tổng quan dữ liệu pilot | 5.687 respondent x 47 cột; tỷ lệ có việc 75,6% | Đọc parquet processed và tính `df.shape`, `df[TARGET].mean()` | Đây là mẫu chính của báo cáo. Tỷ lệ target cao giải thích vì sao baseline đoán lớp đa số đã có accuracy cao. |
| 2 | 11 | Bảng sai số lấy mẫu và bất định | Tỷ lệ có việc analytic 0,7565, urn 0,7563, bootstrap 0,7563; ROC-AUC bootstrap 0,6799; split AUC 0,6811; split accuracy 0,7577 | Module `sampling_variation` tạo nhiều mô phỏng: normal approximation, binomial simulation, bootstrap, random splits | Ba cách ước lượng tỷ lệ có việc gần trùng nhau, CI 95% khoảng 74,5%-76,8%. AUC có CI khoảng 0,644-0,713, xác nhận tín hiệu dự đoán yếu nhưng thật. |
| 3 | 17 | Skip-code -> missing | Các biến kiểm tra (`w04edu`, `w04edu_f`, `w04edu_m`, `y04f308`) có 0 giá trị skip-code bị đổi sang NaN trong ví dụ | Quét các mã skip định nghĩa trước rồi thay bằng missing thật | Minh họa bước làm sạch: phân biệt “không có dữ liệu hợp lệ” với giá trị đo lường thật. |
| 4 | 19 | Kết quả điền khuyết | `w04age` và `y04f308` còn 0 missing; `w04edu_f` còn 3.036 missing; `w04edu_m` còn 2.739 missing; tạo cờ `*_missing` | Dùng median cho biến số, giữ missing logic cho học vấn cha/mẹ và thêm indicator | Cho thấy không phải mọi missing đều nên điền trực tiếp. Với học vấn cha/mẹ, missing có thể mang thông tin về nhánh câu hỏi hoặc cấu trúc dữ liệu. |
| 5 | 21 | Tổng hợp job-spell về cấp cá nhân | Job-spell raw: 27.127 dòng, 9.083 người; sau groupby còn 9.083 dòng, 5 cột; ví dụ gồm `n_job_spells`, `first_start_year`, `last_start_year`, `years_active` | Một người có nhiều dòng lịch sử việc làm, nên groupby `sampid` để đưa về grain một dòng một người | Giải thích cách biến bảng lịch sử việc làm thành đặc trưng cấp cá nhân cho EDA/model. |
| 6 | 23 | Wide -> long cho participation panel | Dạng rộng: 12.213 x 5; dạng dài: 48.852 x 4; số tham gia: wave1 12.213, wave2 10.721, wave3 10.108, wave4 9.665 | Dùng `melt()` để chuyển mỗi cột wave thành dòng respondent-wave | Dạng dài thuận tiện để vẽ attrition và tổng hợp theo wave. Bảng cũng xác nhận không có duplicate/missing flag trong metadata. |
| 7 | 25 | Rời rạc hóa tuổi và regex ngày | Nhóm tuổi: 22-24 = 4.002, 25-27 = 3.897, 28-30 = 3.293, 31+ = 1.021; regex tách năm/tháng; tháng hợp lệ 27.126/27.126 | `pd.cut` tạo age band; regex `str.extract` tách `YYYY-MM` | Minh họa hai kỹ thuật tiền xử lý: discretization cho biến số và parsing văn bản/ngày tháng. |
| 8 | 27 | One-hot/dummy encoding | One-hot có 9 cột, dummy có 7 cột sau `drop_first`; bảng ví dụ gồm `sex_Female`, `sex_Male`, `region_1`... | `pd.get_dummies` chuyển categorical thành biến boolean/0-1 để mô hình dùng được | Cho thấy vì sao biến định danh phải mã hóa trước khi đưa vào mô hình toán học; `drop_first` tránh dư một mức tham chiếu. |
| 9 | 36 | Framing Bernoulli/Binomial cho target | n = 5.687, k = 4.302, p-hat = 0,756462, SE rate = 0,005692, majority baseline accuracy = 0,756462 | Target nhị phân nên có thể xem là Bernoulli; tổng số success tuân theo Binomial | Cảnh báo accuracy không đủ: nếu luôn đoán “employed” đã đạt 75,6%, nên cần ROC-AUC/PR/Brier. |
| 10 | 38 | Nhận diện phân phối lý thuyết | 5 biến số đều bị gắn `Non-normal`; ví dụ tuổi mean 27,19, std 2,46; job_changes skew 1,49; duration skew 1,89 | Tính mean, std, skewness, excess kurtosis, Shapiro p-value | Cung cấp cơ sở thống kê để biết biến nào lệch/đuôi nặng, có nên biến đổi hoặc dùng mô hình ít giả định phân phối. |
| 11 | 40 | Ứng viên biến đổi | `avg_job_duration_months` khuyến nghị `log1p`, `total_jobs` `log1p`, `job_changes` `sqrt`; tuổi/GPA `none` | Thử biến đổi rồi chọn phương án làm `|skew|` nhỏ hơn | Giúp nối EDA với modeling: biến lệch mạnh có thể làm mô hình tuyến tính và khoảng tin cậy kém ổn định nếu dùng dạng thô. |
| 12 | 44 | Kiểm định thống kê với outcome | Welch t-test: 5 biến; chi-square: 9 biến. Có ý nghĩa 5%: tuổi, job_changes, total_jobs, duration, chứng chỉ, vùng, ngành, học vấn cha/mẹ, học vấn, giới tính. GPA p = 0,060; tài sản p = 0,234; job prep p = 0,764 | Numeric so hai nhóm có/không việc bằng Welch t-test; categorical dùng chi-square independence | Kiểm chứng các khác biệt quan sát trong biểu đồ có vượt nhiễu ngẫu nhiên hay không. Tuy vậy p-value không nói cường độ lớn hay nhân quả. |
| 13 | 49 | Ranking subgroup với Wilson CI | Các nhóm ngành hẹp có tỷ lệ thấp: Humanities 33,7%, Education 47,0%, Natural sciences 49,1%; vùng Seoul 79,7%, Gyeongin 78,4%; chứng chỉ Yes 58,3%, No 76,2% | Tính tỷ lệ có việc theo subgroup, Wilson CI 95%, chênh lệch với overall và Cramer's V | Bảng giúp tránh overclaim bằng cách kèm n và CI. Một số subgroup ngành có n nhỏ và chịu selection bias, nên cần đọc thận trọng. |
| 14 | 53 | Catalog đặc trưng | 15 biến ứng viên; 8 đặc trưng vào core model; 3 biến loại vì leakage: `total_jobs`, `job_changes`, `avg_job_duration_months` | Module feature catalog gắn role, dtype, coverage, imputation, encoding, scaling và cờ dùng trong core model | Đây là bảng kiểm toán trước modeling. Nó giải thích vì sao một số biến có liên hệ cao nhưng không được đưa vào model core vì đo sau outcome hoặc có nguy cơ rò rỉ. |
| 15 | 57 | Modeling-ready checks | 4/4 đạt: không trùng `sampid`, target nhị phân 0/1 và tách khỏi X, không leakage trong X, missingness < 100%; X shape = (5.687, 8) | Kiểm tra contract của bảng đầu vào trước khi train model | Đảm bảo dữ liệu huấn luyện đúng grain, đúng target và không vô tình dùng biến sau outcome. Đây là bước kiểm soát chất lượng mô hình. |
| 16 | 59 | Bảng metric test của 4 mô hình | Baseline accuracy 0,7566/AUC 0,5; Logistic L2 accuracy 0,7601/AUC 0,6788/AP 0,8547/Brier 0,1698; Logistic L1 tương tự; Random Forest accuracy 0,6555/AUC 0,6812/AP 0,8534/Brier 0,2202 | Train/test split 4.549/1.138, so sánh baseline, Logistic L2, Logistic L1, Random Forest | Logistic cải thiện AUC rõ so với baseline dù accuracy gần bằng. Random Forest có AUC hơi cao nhất nhưng accuracy/Brier kém hơn, cho thấy xác suất và ngưỡng quyết định chưa tốt. |
| 17 | 68 | Subgroup performance của Logistic L2 | Female AUC 0,689, Male 0,669; vùng Seoul 0,710, Gyeongin 0,635; học vấn junior college 0,655, university 0,688 | Tính metric riêng trên từng subgroup trong test set | Đánh giá công bằng/độ ổn định theo nhóm. Gap lớn nhất là vùng (0,075), nhỏ hơn ở học vấn và giới tính. |
| 18 | 73 | Bias-variance summary | Baseline AUC 0,5; Logistic L2 CV train 0,6943, CV test 0,6736, test 0,6788, gap 0,0207; Logistic L1 gap 0,0198; Random Forest train 0,7378, CV test 0,6712, gap 0,0666 | So sánh AUC train CV, test CV và holdout test | Logistic có gap nhỏ nên kiểm soát phương sai tốt hơn. Random Forest có gap train-CV lớn hơn, dấu hiệu overfitting tương đối dù AUC holdout xấp xỉ logistic. |

## 3. Kết luận đọc nhanh

| Chủ đề | Kết luận |
|---|---|
| Chất lượng mẫu | Mẫu phân tích là 5.687 người sau nhiều bước lọc từ 12.213 người trong khung wave 4; attrition còn 79,1% tới wave 4, nên phải thận trọng khi suy rộng. |
| Outcome | Tỷ lệ có việc cao 75,6%, làm baseline accuracy cao giả tạo. Vì vậy ROC-AUC, PR, Brier và confusion matrix quan trọng hơn accuracy đơn lẻ. |
| Độ phủ biến | Biến lõi như tuổi, giới tính, vùng, học vấn có độ phủ tốt; biến chi tiết về ngành/GPA/loại trường/job prep có độ phủ thấp và chỉ nên dùng trong phân tích hẹp/nhạy cảm. |
| Liên hệ nổi bật | Tuổi là đặc trưng liên hệ mạnh nhất với outcome trong EDA một biến; nhóm ngành, vùng, chứng chỉ và job history cũng có tín hiệu nhưng một số biến bị hạn chế bởi coverage hoặc leakage. |
| Mô hình | Logistic L1/L2 là lựa chọn cân bằng nhất: AUC khoảng 0,679, Brier tốt nhất khoảng 0,170, gap train-CV nhỏ. Random Forest không vượt rõ và có dấu hiệu overfit hơn. |
| Diễn giải | Các biểu đồ/bảng thể hiện liên hệ và năng lực dự đoán trong mẫu pilot, không chứng minh tác động nhân quả. Các biến sau outcome phải loại khỏi core model để tránh rò rỉ. |
